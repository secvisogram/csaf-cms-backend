# Context is relative to ../compose.yaml.

services:
  backend-cms:
    build:
      context: ..
      dockerfile: Dockerfile
    volumes:
      # Advisories placed here are imported once, on startup.
      - "./dev/import:/app/import"
